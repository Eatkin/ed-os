Prompts screen
Check out vaults item - I guess this is basically a note associated with a trajectory
README.md
Import / export data

unify styles

## 2026-07-22: Make level more prominent on Goals screen

**Files:** `style/base.js`, `components/goalsScreen/GoalsScreen.js`

1. Add a new `levelLabel` style to `style/base.js` (20px, bold, accent
   color `#FFB300`, monospace) for the level number.
2. In `GoalsScreen.js`, replace the current "LVL {n}" (plain
   `styles.subtitle`) + bare progress bar block with:
   - A row: big `styles.levelLabel` "LV. {level}" on the left,
     `{currentLevelXP} / {xpToNextLevel} XP` dimmed on the right
     (mirrors `TrajectoryDetailScreen.js`'s existing layout).
   - The existing progress bar, unchanged.
   - A new small caption below the bar: "{xpProgress}% to Level
     {level + 1}" (same treatment as the detail screen's caption).
   - Existing "weekly log count" line stays below, unchanged.

No data-layer changes — `level`, `currentLevelXP`, `xpToNextLevel`,
`xpProgress` are already available on every trajectory object.

Approved via plan mode; proceeding to implement.
